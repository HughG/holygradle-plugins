apply from: "..\\..\\..\\testUtil\\header.gradle"
configurations {
    foo
    bar
}
// Declare a packed dependency for this root project
packedDependencies {
    "ext_dep_with_config_foo_requiring_1_0_compile" {
        dependency "holygradle.test:external-lib:1.0"
        configuration "foo->compile"
    }
}
sourceDependencies {
    // Declare a source dependency that requires a conflicting version
    // of "external-lib".  We want to test that this is detected as a conflict
    "src_dep" {
        hg "http://a_dummy_url_that_we_wont_need"
        publishing.configuration "foo->otherChildConfig"
    }
}    