package holygradle

import org.junit.Test
import static org.junit.Assert.*

class SvnRepositoryTest {
    @Test
    public void testSvn() {
        /*
        SourceControlRepository repo = SourceControlRepositories.get(new File("C:\\Projects\\CAP"))
        assertTrue(repo instanceof SvnRepository)
        assertTrue(repo.getRevision() == "33707")
        assertFalse(repo.hasLocalChanges())
        def url = repo.getUrl()
        println "url: $url"
        assertTrue(url == "http://edisubversion/svn/BcdeFramework/CAP/branches/2012_07")
        */
    }
}