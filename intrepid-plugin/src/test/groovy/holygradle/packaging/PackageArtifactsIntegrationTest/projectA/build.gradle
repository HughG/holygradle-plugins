// NOT using gplugins.apply(), because we want to test programmatic application of the intrepid plugin.

configurations {
    foo
    bar.extendsFrom foo
}

packageArtifacts {
    foo {
        include "**/f*"
    }
    bar {
        include "**/b*"
    }
}
